<% if (alerts.error) { %>
  <% alerts.error.forEach(eachError => { %>
    <div class="alert alert-error"></div>
 <% }) %>
<% } %>

<% if (alerts.success) { %>
<% alerts.success.forEach(eachError => { %>
<div class="alert alert-success"></div>
<% }) %>
<% } %>
